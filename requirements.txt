# ETF 量化分析平台（MVP）最小依赖

# 运行时：数据获取与处理
akshare>=1.11.0,<2.0.0         # 中国金融数据接口
pandas>=2.2.0,<3.0.0           # 数据处理与分析
numpy>=1.24.3,<2.0.0           # 数值计算

# 配置与日志、CLI 输出
pyyaml>=6.0,<7.0               # YAML 配置
loguru>=0.7.0,<1.0.0           # 日志
click>=8.1.3,<9.0.0            # 命令行
rich>=13.3.5,<14.0.0           # 终端美化输出

# 历史数据缓存（Parquet）
pyarrow>=12.0.0,<22.0.0

# 测试依赖
pytest>=7.3.1,<8.0.0           # 单元测试
pytest-cov>=4.1.0,<5.0.0       # 覆盖率
pytest-mock>=3.10.0,<4.0.0     # mocking 支持
freezegun>=1.2.2,<2.0.0        # 可选：时间冻结
python-dotenv>=1.0.0,<2.0.0    # 环境变量管理

{
  "name": "sora",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "pnpm -r run test",
    "build": "pnpm -r run build",
    "lint": "eslint .",
    "format": "prettier --write .",
    "sora": "pnpm --filter @sora/cli run start"
  },
  "devDependencies": {
    "eslint": "^9.15.0",
    "prettier": "^3.3.0",
    "typescript": "^5.5.0",
    "typescript-eslint": "^8.0.0"
  },
  "pnpm": {
    "onlyBuiltDependencies": ["better-sqlite3", "esbuild", "js-tiktoken"]
  }
}
